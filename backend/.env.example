# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/paper_trading"

# Server port (default 3001)
PORT=3001

# Demo user id (set after seeding; used so frontend always uses same user)
DEMO_USER_ID=
