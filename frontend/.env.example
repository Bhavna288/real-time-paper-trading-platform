# In dev, the Vite proxy forwards /api and /socket.io to the backend (localhost:3001).
# No env vars required for local development.

# Optional: override API/socket base URL for production or different backend.
# VITE_API_BASE_URL=http://localhost:3001
